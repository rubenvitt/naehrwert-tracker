# =============================================================================
# Nährwert-Tracker Backend - Environment Variables
# =============================================================================
# 1. Kopiere zu `stack.env`
# 2. Führe `./setup.sh` aus um Tokens zu generieren
# 3. Trage deinen OpenRouter API Key ein
# =============================================================================

# -----------------------------------------------------------------------------
# [MANUAL] - Muss manuell eingetragen werden
# -----------------------------------------------------------------------------

# OpenRouter API Key (https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-...

# -----------------------------------------------------------------------------
# [GENERATED] - Werden von setup.sh generiert
# -----------------------------------------------------------------------------

# API Tokens für User-Authentifizierung
# Format: token:username:rateLimit,token2:username2:rateLimit2
API_TOKENS=GENERATED_BY_SCRIPT

# -----------------------------------------------------------------------------
# [STATIC] - Projekt-Konfiguration
# -----------------------------------------------------------------------------

# Projekt-Name (für Docker + Traefik)
COMPOSE_PROJECT_NAME=naehrwert

# Backend Domain
API_DOMAIN=naehrwert-api.rubeen.dev

# Frontend Domain (für CORS)
CORS_ORIGIN=https://essen-tracker.rubeen.dev

# Backend Port (intern)
BACKEND_PORT=3000
